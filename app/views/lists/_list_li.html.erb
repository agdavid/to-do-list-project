  <!-- add strikethrough styling to class if complete -->
  <%= li_for_item(item) do %> 
    <div class="view"> 
    <!-- check_box form to POST lists/:list_id/items/:id ; items#update -->
      <%= form_for_item_status(item) %>

      <label><%= item.description %></label>
    </div>
  <% end %>